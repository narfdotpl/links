#!/usr/bin/env sh

VENV=$HOME/narf.pl/ln/env/ln
PYTHON_VERSION=python2.7
SUFFIX=lib/$PYTHON_VERSION/site-packages

export PATH=$VENV/bin:$PATH
export PYTHONPATH=$VENV/$SUFFIX:$HOME/.python/$SUFFIX:/usr/local/$PYTHON_VERSION/$SUFFIX
export PIP_DOWNLOAD_CACHE=$HOME/.pip/download-cache
